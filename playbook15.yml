---
- name: Using loops in asible
  hosts: all
  tasks:
    - name: Install s/w app's
      apt: 
        name: "{{item}}"
        state: present
        update_cache: yes
      with_items:
        - tree
        - git
        - maven
...
